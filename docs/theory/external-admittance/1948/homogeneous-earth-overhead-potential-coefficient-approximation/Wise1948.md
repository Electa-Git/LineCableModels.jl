# Wise analytical approximations to the overhead potential-coefficient correction

## Identification and source

| Field | Value |
| --- | --- |
| Family | External admittance |
| Geometry | Infinite straight thin parallel wire(s); the approximation variables use ``h+z`` and ``y``. |
| Calculated quantities | Analytical approximations to the complex earth correction ``M+jN`` used in overhead self and mutual potential coefficients |
| Earth structure | Homogeneous flat half-space. |
| Model and approximation | The parent integral is [Wise's equation (8)]. The source expands ``\sqrt{t^2+u^2}=t+u-tu/(t+u)+\cdots``, retains the displayed truncated expression, performs partial fractions with roots ``r_1,r_2``, and obtains (9). Equation (10) imposes ``y=0``. The final unnumbered form further assumes ``g`` is very small. The source gives no formal remainder bound; its reported sub-one-percent discrepancy applies only to checked points. |
| Main source | W. Howard Wise (1948) |
| Citation key(s) | Primary: `:Wise1948`; later MKS applications: `:Nakagawa1981a`, `:Nakagawa1981b` |
| Evidence status | Original PDF page images checked; later applications retain the same correction |

**Description.** Wise's analytical approximations to the integral correction ``M+jN`` in the homogeneous-earth overhead potential coefficient, including the general exponential-integral form, its ``y=0`` specialization, and two coarser ``y=0`` forms.

**Assumptions.**

| Field | Treatment | Evidence |
| --- | --- | --- |
| Impressed longitudinal propagation constant ``Γ`` | Inherited from the parent integral: source ``\gamma`` is assigned ``jk`` as a first approximation with dependence ``e^{-\gamma x}``. | Stated — printed p. 367. |
| Air propagation constant ``γ_air`` | ``k=2\pi/\lambda``; inherited from the parent integral. | Stated — printed p. 366. |
| Earth propagation constant ``γ_earth`` | Inherited source definition ``k_2^2=\omega^2\mu\varepsilon-j4\pi\omega\mu\sigma`` in electromagnetic cgs units. | Stated — printed p. 366. |
| Earth permittivity and displacement current | ``\varepsilon`` and ``\sigma`` are retained through ``a=\varepsilon-j2c\lambda\sigma`` and ``u``. | Equation-implied — definitions preceding (9), printed p. 370. |
| Range of validity | Equation (10) is restricted to ``y=0``. The source reports less than one percent discrepancy for both ``M`` and ``N`` at an unspecified set of checked points. The final expression additionally requires ``g`` to be “very small”; no numerical bound is stated. | Stated — printed pp. 370–371. |
| Earth permeability ``μ_earth`` | Unit permeability everywhere in electromagnetic units. | Stated — printed p. 367. |
| Arrangement | Overhead; parent formula covers self and mutual, while (10) and the coarser forms set transverse separation ``y=0``. | Stated — printed pp. 365, 370–371. |
| Earth structure | Homogeneous flat half-space. | Stated — printed pp. 365–366. |
| Conductor and insulation geometry | Infinite straight thin parallel wire(s); the approximation variables use ``h+z`` and ``y``. | Stated/equation-implied — Fig. 1 and definitions preceding (9). |
| Constitutive and field assumptions | Parent harmonic homogeneous-medium assumptions retained. The approximation replaces a square root algebraically and then partially fractions the result. | Stated — derivation preceding (9), printed p. 370. |
| Conventions | Time factor ``e^{j\omega t}``, longitudinal ``e^{-\gamma x}``; cgs units; ``\mathop{\mathrm{li}}(e^{-z})=-\int_z^\infty e^{-t}/t\,dt`` with ``-\pi\leq\arg z\leq\pi``. | Stated — printed pp. 366, 370. |

**Expression.** Source equations (9)–(10) and the two unnumbered coarser approximations on printed p. 371.

```math
\begin{aligned}
u^2&=j e^{j2\eta} \\
u&=e^{j(\eta+\pi/4)} \\
a&=\varepsilon-j2c\lambda\sigma=1/\tau^2, \\
g'&=(h+z-jy)\xi\sqrt{\alpha} \\
g''&=(h+z+jy)\xi\sqrt{\alpha} \\
g&=(h+z)\xi\sqrt{\alpha}, \\
r_1&=\frac{u}{2}\left(1-\sqrt{1-\frac{4}{a+1}}\right) \\
r_2&=\frac{u}{2}\left(1+\sqrt{1-\frac{4}{a+1}}\right), \\
M+jN&\approx
\frac{1}{2(a+1)(r_2-r_1)}
\left[
-r_2e^{g'r_1}\mathop{\mathrm{li}}(e^{-g'r_1})
+r_1e^{g'r_2}\mathop{\mathrm{li}}(e^{-g'r_2})
-r_2e^{g''r_1}\mathop{\mathrm{li}}(e^{-g''r_1})
+r_1e^{g''r_2}\mathop{\mathrm{li}}(e^{-g''r_2})
\right],
\end{aligned}\qquad\text{(9)}
```

```math
M+jN\approx
\frac{-r_2e^{gr_1}\mathop{\mathrm{li}}(e^{-gr_1})
      +r_1e^{gr_2}\mathop{\mathrm{li}}(e^{-gr_2})}
     {(a+1)(r_2-r_1)},
\qquad y=0.
\qquad\text{(10)}
```

The first unnumbered zero-separation approximation is

```math
M+jN\approx\int_0^\infty\frac{e^{-gt}}{u+at}\,dt
=-\frac{1}{a}e^{gu/a}\mathop{\mathrm{li}}(e^{-gu/a}),
\qquad y=0.
```

The further small-``g`` approximation is

```math
\begin{aligned}
M+jN&\approx\int_0^1\frac{dt}{u+at}
+\int_1^\infty\frac{e^{-gt}}{(1+a)t}\,dt \\
&=\frac{1}{a}\log\left(1+\frac{a}{u}\right)
-\frac{1}{1+a}\mathop{\mathrm{li}}(e^{-g}),
\qquad y=0,\quad g\ll1.
\end{aligned}
```

The source defines ``\mathop{\mathrm{li}}(e^{-z})=-\int_z^\infty e^{-t}/t\,dt`` and provides both a power series and an asymptotic expansion for its evaluation.

**Approximation.** The parent integral is [Wise's equation (8)](../homogeneous-earth-overhead-potential-coefficient/Wise1948.md). The source expands ``\sqrt{t^2+u^2}=t+u-tu/(t+u)+\cdots``, retains the displayed truncated expression, performs partial fractions with roots ``r_1,r_2``, and obtains (9). Equation (10) imposes ``y=0``. The final unnumbered form further assumes ``g`` is very small. The source gives no formal remainder bound; its reported sub-one-percent discrepancy applies only to checked points.

**Numerical interpretation.** In SI units, set ``a=\gamma_g^2/\gamma_a^2`` and ``\beta=\sqrt{\gamma_g^2-\gamma_a^2}``. The source scale is ``\xi\sqrt{\alpha}=|\beta|``, so ``u=\beta/|\beta|``. The physical partial-fraction roots are ``q_i=|\beta|r_i``, and their exponential-integral arguments are ``(H\pm jx)q_i``. Equation (7) of the parent record fixes the matrix normalization:

```math
P_{mn}=\frac{\ln(D_{mn}/d_{mn})+2(M+jN)}{2\pi\varepsilon_a}.
```

The general rational selection evaluates (9), including (10) at ``x=0``. The smaller root is evaluated from ``q_1q_2=\beta^2/(a+1)`` to retain accuracy when the roots have very different magnitudes. The scaled exponential integral is continued from its real-axis Laplace representation when the separation angle crosses the principal cut of the product argument. This avoids an artificial discontinuity without changing the rational approximation.

Separate coarse and small-``g`` selections evaluate the two unnumbered formulas. Both require zero horizontal separation. The small-``g`` selection rejects ``H|\beta|\ge1``; the remaining accepted interval is not an accuracy guarantee. Self terms use ``x=0``, ``D=2h``, and ``d=r``. The complete potential matrix is inverted only after assembly.

**Limitations.** Equations (10) and both coarser formulas do not cover nonzero transverse separation. The tested points and parameter bounds for the reported error are not tabulated. Complex square-root and logarithmic-integral branches are not fully specified beyond ``-\pi\leq\arg z\leq\pi`` for the displayed ``\mathop{\mathrm{li}}`` expansion.

**Reference.** [Wise1948](@cite), equations (9)–(10), printed p. 370; unnumbered coarser forms, printed p. 371.

**Transcription source.** Original publication PDF. Definitions, signs, four exponential-integral terms in (9), the ``y=0`` reduction, and both p. 371 integrals were checked against rendered page images.

## Source transcription

The operation printed immediately before (9) is

```math
\sqrt{t^2+u^2}=t+u-\frac{tu}{t+u}+\cdots,
```

followed by

```math
\frac{1}{\sqrt{t^2+u^2}+at}
\approx\frac{t+u}{(a+1)t^2+(a+1)tu+u^2}
=\frac{t+r_1+r_2}{(a+1)(t+r_1)(t+r_2)}.
```

This record keeps the source's ``\approx`` relation and does not upgrade it to equality.

## Notation map

| Source symbol | Display symbol | Physical meaning | Units/convention |
| --- | --- | --- | --- |
| ``M+jN`` | unchanged | Complex earth correction in the parent potential coefficient | dimensionless normalization |
| ``a`` | unchanged | ``\varepsilon-j2c\lambda\sigma=1/\tau^2`` | complex dimensionless source parameter |
| ``u`` | unchanged | Square-root auxiliary, ``u^2=j e^{j2\eta}`` | complex dimensionless |
| ``r_1,r_2`` | unchanged | Partial-fraction roots | complex dimensionless |
| ``g',g'',g`` | unchanged | Scaled source/observation distances | complex dimensionless |
| ``\mathop{\mathrm{li}}`` | unchanged | Source logarithmic-integral convention | definition printed below (10) |
| ``h,z,y`` | unchanged | Source height, observation height, transverse separation | cgs length |
| ``\varepsilon,\sigma,c,\lambda`` | unchanged | Earth dielectric constant, conductivity, light speed, wavelength | source cgs conventions |

## Evidence and approximation sources

- Parent integral and scaling variables: equation (8), printed pp. 365 and 369.
- Square-root truncation, partial fraction, and general approximation: equation (9), printed p. 370.
- ``y=0`` specialization, logarithmic-integral definition, series, asymptotic evaluator, and numerical comparison: equation (10) and following text, printed p. 370.
- Coarser ``y=0`` and “very small” ``g`` approximations: unnumbered expressions, printed p. 371.

## Limitations and discrepancies

- The source says (9) is “fairly good” and reports under one percent discrepancy at the checked points but supplies neither a global domain nor a global error bound.
- The printed asymptotic remainder is denoted ``R_n`` without a bound in the inspected page.
- No equation-preserving Markdown was available; OCR text damages several radicals and exponential-integral terms.
